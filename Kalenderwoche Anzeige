(function () {
  'use strict';

  function getISOWeekNumber(date) {
    const tempDate = new Date(date.getTime());
    tempDate.setHours(0, 0, 0, 0);
    tempDate.setDate(tempDate.getDate() + 3 - (tempDate.getDay() + 6) % 7);
    const firstThursday = new Date(tempDate.getFullYear(), 0, 4);
    const weekNumber = Math.round((tempDate - firstThursday) / (7 * 24 * 60 * 60 * 1000)) + 1;
    return weekNumber;
  }

  // ─── QUIP ────────────────────────────────────────────────────────────────────

  function injectKWButtonQuip() {
    const buttonGroup = document.querySelector('.buttons[role="group"]');
    if (!buttonGroup) return;
    if (document.getElementById('kw-quip-button')) return;

    const kw = getISOWeekNumber(new Date());
    const kwWrapper = document.createElement('div');
    kwWrapper.id = 'kw-quip-button';
    kwWrapper.style.display = 'inline-flex';
    kwWrapper.style.alignItems = 'center';

    const kwButton = document.createElement('button');
    kwButton.className = 'button button-flex clickable dark';
    kwButton.setAttribute('data-mousedown', 'no-caret-move');
    kwButton.setAttribute('aria-label', `Kalenderwoche ${kw}`);
    kwButton.style.cssText = `
      background-color: rgb(var(--quipAction));
      height: 32px; padding-left: 12px; padding-right: 12px;
      color: #fff; fill: #fff; border: none; border-radius: 4px;
      cursor: default;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      font-size: 14px; font-weight: 500; line-height: 32px;
      display: flex; align-items: center; gap: 6px;
      margin-right: 4px; user-select: none;
    `;

    const iconSvg = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
    iconSvg.setAttribute('viewBox', '0 0 24 24');
    iconSvg.setAttribute('width', '16');
    iconSvg.setAttribute('height', '16');
    iconSvg.style.fill = 'currentColor';
    iconSvg.style.flexShrink = '0';
    iconSvg.innerHTML = `<path d="M19 4h-1V2h-2v2H8V2H6v2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 16H5V10h14v10zM9 14H7v-2h2v2zm4 0h-2v-2h2v2zm4 0h-2v-2h2v2z"/>`;

    const textSpan = document.createElement('span');
    textSpan.textContent = `KW ${kw}`;
    kwButton.appendChild(iconSvg);
    kwButton.appendChild(textSpan);
    kwWrapper.appendChild(kwButton);

    const shareButton = buttonGroup.querySelector('.change-access-button');
    if (shareButton && shareButton.parentNode === buttonGroup) {
      buttonGroup.insertBefore(kwWrapper, shareButton);
    } else {
      buttonGroup.appendChild(kwWrapper);
    }
  }

  // ─── SHAREPOINT ──────────────────────────────────────────────────────────────

  function applyWhite(el) {
    // setProperty mit 'important' schlägt JEDES externe CSS
    el.style.setProperty('color', '#ffffff', 'important');
    el.style.setProperty('fill', '#ffffff', 'important');
  }

  function createKWBadge() {
    const kw = getISOWeekNumber(new Date());

    const wrapper = document.createElement('div');
    wrapper.id = 'kw-sharepoint-badge';

    // Exakt gleiche Struktur wie Bh46b1rd8BrU4zolL9RzUg== Container (height:100%, line-height:48px, display:flex)
    wrapper.style.setProperty('height', '100%', 'important');
    wrapper.style.setProperty('line-height', '48px', 'important');
    wrapper.style.setProperty('display', 'flex', 'important');
    wrapper.style.setProperty('align-items', 'center', 'important');
    wrapper.style.setProperty('justify-content', 'center', 'important');
    wrapper.style.setProperty('padding', '0 8px', 'important');
    wrapper.style.setProperty('cursor', 'default', 'important');
    wrapper.style.setProperty('user-select', 'none', 'important');
    wrapper.style.setProperty('flex-shrink', '0', 'important');
    wrapper.style.setProperty('color', '#ffffff', 'important');
    wrapper.style.setProperty('fill', '#ffffff', 'important');
    wrapper.style.setProperty('box-sizing', 'border-box', 'important');

    const inner = document.createElement('div');
    inner.style.setProperty('display', 'flex', 'important');
    inner.style.setProperty('align-items', 'center', 'important');
    inner.style.setProperty('gap', '5px', 'important');
    inner.style.setProperty('color', '#ffffff', 'important');
    inner.style.setProperty('fill', '#ffffff', 'important');

    const svg = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
    svg.setAttribute('viewBox', '0 0 24 24');
    svg.setAttribute('width', '16');
    svg.setAttribute('height', '16');
    svg.setAttribute('aria-hidden', 'true');
    svg.style.setProperty('fill', '#ffffff', 'important');
    svg.style.setProperty('flex-shrink', '0', 'important');
    svg.style.setProperty('display', 'block', 'important');
    svg.innerHTML = `<path d="M19 4h-1V2h-2v2H8V2H6v2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 16H5V10h14v10zM9 14H7v-2h2v2zm4 0h-2v-2h2v2zm4 0h-2v-2h2v2z"/>`;

    const text = document.createElement('span');
    text.style.setProperty('font-family', '"Segoe UI", -apple-system, BlinkMacSystemFont, sans-serif', 'important');
    text.style.setProperty('font-size', '13px', 'important');
    text.style.setProperty('font-weight', '700', 'important');
    text.style.setProperty('color', '#ffffff', 'important');
    text.style.setProperty('white-space', 'nowrap', 'important');
    text.style.setProperty('line-height', '48px', 'important');
    text.style.setProperty('-webkit-font-smoothing', 'antialiased', 'important');
    text.textContent = `KW ${kw}`;

    inner.appendChild(svg);
    inner.appendChild(text);
    wrapper.appendChild(inner);
    return wrapper;
  }

  function injectKWButtonSharePoint() {
    if (document.getElementById('kw-sharepoint-badge')) return;

    // Beide Elemente müssen existieren und Geschwister sein
    const rightRegion = document.getElementById('O365_HeaderRightRegion');
    const headerRegion = document.getElementById('HeaderButtonRegion');

    if (!rightRegion) return; // noch nicht geladen, Observer holt es nach

    // Prüfe ob sie wirklich Geschwister sind
    if (rightRegion.parentNode && headerRegion &&
        rightRegion.parentNode === headerRegion.parentNode) {
      // Perfekt: zwischen HeaderButtonRegion und O365_HeaderRightRegion
      rightRegion.parentNode.insertBefore(createKWBadge(), rightRegion);
      return;
    }

    // Fallback: direkt vor rightRegion egal wo
    if (rightRegion.parentNode) {
      rightRegion.parentNode.insertBefore(createKWBadge(), rightRegion);
    }
  }

  // ─── ROUTING ─────────────────────────────────────────────────────────────────

  function injectKWButton() {
    const host = window.location.hostname;
    if (host.includes('quip.com')) {
      injectKWButtonQuip();
    } else if (host.includes('sharepoint.com')) {
      injectKWButtonSharePoint();
    }
  }

  // ─── KW AKTUALISIERUNG ───────────────────────────────────────────────────────

  setInterval(() => {
    const newKw = getISOWeekNumber(new Date());
    const quipSpan = document.querySelector('#kw-quip-button span');
    if (quipSpan) quipSpan.textContent = `KW ${newKw}`;
    const spText = document.querySelector('#kw-sharepoint-badge span');
    if (spText) spText.textContent = `KW ${newKw}`;
  }, 3600000);

  // ─── OBSERVER ────────────────────────────────────────────────────────────────

  let injected = false;

  const observer = new MutationObserver(() => {
    const badgeId = window.location.hostname.includes('quip.com')
      ? 'kw-quip-button' : 'kw-sharepoint-badge';
    if (injected && document.getElementById(badgeId)) return;
    injected = false;
    injectKWButton();
    injected = !!document.getElementById(badgeId);
  });

  observer.observe(document.body, { childList: true, subtree: true });

  injectKWButton();
  setTimeout(injectKWButton, 500);
  setTimeout(injectKWButton, 1500);
  setTimeout(injectKWButton, 3000);

})();
